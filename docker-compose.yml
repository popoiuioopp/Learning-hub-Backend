# Docker Compose file Reference (https://docs.docker.com/compose/compose-file/)

version: '3'

# Define services
services:

  lbserver:
    # Configuration for building the docker image for the service
    build:
      context: ./lbserver # Use an image built from the specified dockerfile in the current directory.
      dockerfile: Dockerfile
    container_name: lbserver
    restart: unless-stopped
    ports:
      - "5000:5000" # Forward the exposed port 5000 on the container to port 5000 on the host machine
    depends_on:
      - server1  # This service depends on server1. Start that first.
      - server2 # This service depends on server2. Start that first.
      - mysql # This service depends on mysql. Start that first.
      - redis # This service depends on redis. Start that first.
    environment: # Pass environment variables to the service
      REDIS_URL: redis:6379
    networks: # Networks to join (Services on the same network can communicate with each other using their name)
      - backend

  server1:
    build:
      context: ./server-1
      dockerfile: Dockerfile
    container_name: server-1
    restart: unless-stopped
    ports:
      - "5001:5001"
    depends_on: 
      - mysql 
      - redis 
    networks:
      - backend
  
  server2:
    build:
      context: ./server-2
      dockerfile: Dockerfile
    container_name: server-2
    restart: unless-stopped
    ports:
      - "5002:5002"
    depends_on: 
      - mysql 
      - redis 
    networks:
      - backend

  # MySQL Service
  mysql:
    image: mysql
    container_name: mysql
    volumes:
      - container-volume:/var/lib/mysql
      - ./mysql/mysqldump.sql:/docker-entrypoint-initdb.d/mysqldump.sql
    environment:
      MYSQL_ROOT_PASSWORD: FgTQTzNM62cC63K
      MYSQL_DATABASE: learninghub
    ports:
      - "3306:3306"
    networks:
      - backend

  # Redis Service
  redis:
    image: "redis:alpine" 
    container_name: redis
    restart: unless-stopped
    networks:
      - backend

volumes:
  container-volume:

networks:
  backend: